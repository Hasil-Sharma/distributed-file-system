#include <stdbool.h>
#include <stdio.h>
#include <string.h>

#ifndef NETUTILS_H
#define NETUTILS_H

enum CommonConstants {
  LIST_FLAG = 0,
  GET_FLAG = 1,
  PUT_FLAG = 2,
  MKDIR_FLAG = 3
};

#endif
